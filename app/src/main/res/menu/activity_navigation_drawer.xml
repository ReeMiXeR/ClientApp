<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/nav_camara" android:icon="@android:drawable/ic_menu_search"
            android:title="Search" />
        <item android:id="@+id/nav_gallery" android:icon="@android:drawable/ic_menu_agenda"
            android:title="Ordered" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item android:id="@+id/about_us" android:icon="@android:drawable/ic_menu_share"
                android:title="About us" />
        </menu>
    </item>

</menu>